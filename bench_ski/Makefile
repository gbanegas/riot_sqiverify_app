include ../Makefile.tests_common

MAIN_THREAD_SIZE = THREAD_STACKSIZE_DEFAULT+20000
CFLAGS += -DTHREAD_STACKSIZE_MAIN=\($(MAIN_THREAD_SIZE)\)
CFLAGS += -DSQISIGN_SIGS_STACKSIZE=1280000
#CFLAGS += -DTHREAD_STACKSIZE_MAIN=20000
USEMODULE += embunit
USEMODULE += xtimer
USEMODULE += random
USEPKG += sqisign



include $(RIOTBASE)/Makefile.include
